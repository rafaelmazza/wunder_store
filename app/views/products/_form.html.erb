<%= form_for @product do |f| %>
	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :description %>
		<%= f.text_area :description %>
	</p>
	<p>
		<%= f.label :price %>
		<%= f.text_field :price %>
	</p>
  <%= f.fields_for :option_types do |ot_form| %>
    <%= render 'option_types/form', :f => ot_form %>
  <% end %>
  
  <!--
  <fieldset>
	  <h2>Variants</h2>
	  <%= f.fields_for :variants do |variants_form| %>
      <p>
        <%= variants_form.label :price %>
        <%= variants_form.text_field :price %>
      </p>
    <% end %>   
	</fieldset>
  -->
  
	<p>
		<%= f.submit 'Save' %>
	</p>
<% end %>